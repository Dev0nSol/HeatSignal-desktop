name: Windows Build (placeholder)

on:
  workflow_dispatch: {}

# This is a safe, no-op workflow until code exists.
# When you're ready to build:
#  1) swap this with a Tauri + PyInstaller build workflow
#  2) have the job upload the installer as an artifact
#  3) copy its SHA-256 into manifests/latest.json and sign it (see UPDATES.md)

jobs:
  noop:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Print repo status
        run: |
          echo "Repo checked out."
          echo "This workflow is a placeholder and won't try to build yet."
          echo "Enable your real build steps when the code is added."
